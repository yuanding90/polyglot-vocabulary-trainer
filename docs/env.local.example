## Example local environment file (copy as .env.local at project root)

# Supabase (Local or Staging for development)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=replace-with-local-anon-key

# OAuth redirect base for local/LAN testing (falls back to window.location.origin)
NEXT_PUBLIC_AUTH_REDIRECT_BASE=http://localhost:3000

# Similar Words panel configuration (client)
NEXT_PUBLIC_MAX_SIMILAR_WORDS=5

# AI provider keys (server-side only; do not expose real keys to client)
# Used by server Route Handlers / Supabase Edge Functions
ANTHROPIC_API_KEY=replace-with-server-key


